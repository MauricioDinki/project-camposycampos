{% extends "partials/base.njk" %}

{% block title %}Especialistas en derecho penal:{% endblock %}

{% set view_name = 'home' %}

{% block content %}
<section class="home">
  <section class="home-cover">
    <div class="home-cover-carousel owl-carousel">
      {% for carousel in es.home.cover.carousel %}
      <div class="{{carousel.slideTwoClass}}">
        <span class="home-cover-intro {{carousel.slideTwoIntroClass}} helveneue-bold">{{es.home.cover.introPart1}}<span class="helveneue">{{es.home.cover.introPart2}}</span></span>
        <h2 class="home-cover-carousel-title bebasneue-bold">{{carousel.textPart1}}{% if carousel.textPart2 %}<br>{{carousel.textPart2}}{% endif %}</h2>
        {% if carousel.desc %}<h3 class="home-cover-carousel-subtitle helveneue">{{carousel.desc}} <span class="helveneue">{{carousel.descPart2}}</span></h3>{% endif %}
        {% if carousel.logo %}<img class="home-cover-carousel-logo" src="{{carousel.logo}}">{% endif %}
        <a href="{{carousel.btnLink}}">
          <button class="btn helveneue-medium">{{carousel.btn}}</button>
        </a>
      </div>
      {% endfor %}
    </div>
  </section>

  <section class="home-services">
    <h2 class="home-services-title helveneue-bold">{{es.home.services.title}}</h2>
    <div class="home-services-carousel owl-carousel">
      {% for service in es.home.services.carousel %}
      <div class="home-services-content">
        <img class="home-services-img" src="{{service.img}}">
        <span class="home-services-desc helveneue-bold">{{service.desc}}</span>
      </div>
      {% endfor %}
    </div>
  </section>

  <section class="home-compliance">
    <h2 class="home-compliance-title helveneue-bold">{{es.home.compliance.title}}</h2>
    <div class="home-compliance-info">
      <h3 class="home-compliance-subtitle bebasneue-bold">{{es.home.compliance.subtitlePart1}}<br><span>{{es.home.compliance.subtitlePart2}}</span></h3>
      <div class="home-compliance-btns">
        <a href="{{es.home.compliance.complianceLink}}">
          <button class="btn helveneue-medium">{{es.home.compliance.complianceBtn}}</button>
        </a>
        <a href="{{es.home.contactLink}}">
          <button class="btn helveneue-medium">{{es.home.compliance.btn}}</button>
        </a>
      </div>
    </div>
    <div class="home-compliance-img-container">
      {% for img in es.home.compliance.images %}
      <img class="home-compliance-img {{img.itemClass}}" src="{{img.item}}">
      {% endfor %}
    </div>
  </section>

  <section class="home-blog">
    <img class="home-blog-img" src="{{es.home.blog.img}}">
    <div class="home-blog-headers">
      <h3 class="home-blog-title helveneue-bold">{{es.home.blog.title}}</h3>
      <h4 class="home-blog-subtitle bebasneue-bold">{{es.home.blog.subtitle}}</h4>
    </div>
    <div class="home-blog-card"></div>
  </section>
</section>
{% endblock %}
